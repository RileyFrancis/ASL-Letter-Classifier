# ASL-Letter-Classifier

### Installation
First, you will need to create an Anaconda environment. If you do not have conda installed, please refer to https://www.anaconda.com/download.
```bash
conda env create -f environment.yml
conda activate asl
```